# Copy to .env and tweak. docker-compose reads this automatically.

# Port the web container publishes on the host. Default 80 for a
# straight HTTP deploy. Use something like 8080 when another service
# already owns 80, or "127.0.0.1:8080" when a host-level reverse proxy
# fronts the app.
PORT=80
