"""HTTP API layer for the CO Attainment calculator (Phase 2).

The API is a thin JSON wrapper around :mod:`app.calculator`. It owns no
domain logic — just validation, file handling, and serialization.
"""
